-- =========================================================
--  СКЛАД · замовлення від постачальника (2026-09-09)
--  Новий статус замовлення 'ordered' («Їде») між 'submitted' і 'shipped':
--  Оля позначає, що замовила товар у постачальників.
-- =========================================================
alter table public.supply_orders add column if not exists ordered_at timestamptz;
