-- =========================================================
--  ЗАДАЧІ · окреме підтвердження ознайомлення (2026-09-09)
--  ack: { cabinet_key: timestamp } — виконавець тисне «Ознайомлений»
--  у модалці, що не зникає, поки не підтвердить. Окремо від seen
--  (seen проставляється просто при відкритті вкладки «Задачі»).
-- =========================================================
alter table public.tasks add column if not exists ack jsonb not null default '{}'::jsonb;
